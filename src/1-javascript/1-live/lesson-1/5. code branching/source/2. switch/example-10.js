const color = 'green'; // 'red', 'yellow', 'green'

switch (color) {
    case 'green': {
        // if..else
        if (true) {

        }

        // Loops
        for (let index = 0; index < array.length; index++) {
            //
        }


        // functions
        function name(params) {
            
        }

        // switch..case
        switch (key) {
            case value:
                
                break;
        
            default:
                break;
        }
        break;
    }
    default:
        console.log("I don't know this color!");
}

